running list of the python libraries we need:

----DISCORD PYTHON PACKAGE----              (to read incoming messages in discord)
py -3 -m pip install -U discord.py

----SELENIUM PACKAGE----                    (to send our messages as an actual user)
pip install selenium
pip install webdriver-manager